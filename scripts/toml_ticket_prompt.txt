Implement ticket $ticket_id — $ticket_title at:
$ticket_path

Run identity:
- nonce: $nonce
- repository root: $repo_root
- fixed review/base commit: $base_commit
- machine-readable receipt: $receipt_path

Normative sources:
- $implementation_spec
- $design_spec
- $interface_freeze
- the active ticket and its resolved blockers

Follow the loaded implement skill exactly, with these additional execution requirements:

1. Read the ticket, normative sources, repository context/ADRs, and current code. Make and record a concise implementation plan before editing. Do not ask the user to reconfirm the public test seams: the approved public `toml` and `temporal` interfaces and the ticket's explicitly permitted test-only probes are the pre-agreed seams.
2. Implement in vertical red-green TDD slices. For every slice, observe a meaningful red failure before the minimal green implementation. Run focused tests and typechecking frequently, not only at the end.
3. Do not weaken a frozen API, ownership rule, diagnostic, canonical byte, strictness rule, feasibility gate, or acceptance requirement. If the ticket cannot be completed exactly, stop as blocked instead of guessing.
4. Before committing, run an adversarial pre-commit review of the complete uncommitted diff from both Standards and Spec perspectives using fresh review context. Fix every blocking/high-confidence issue worth fixing now, then rerun focused and full applicable tests.
5. Update only this ticket: change `**Status:** ready-for-agent` to `**Status:** resolved` and check every acceptance box. Do not alter its title, blocker line, criterion wording, or any other ticket.
6. Create exactly one non-merge commit for this ticket with subject `ticket $ticket_id: $ticket_title`. Do not create intermediate commits, rewrite history before $base_commit, or push.
7. After that commit, satisfy the implement skill's code-review requirement: read and follow the code-review skill using $base_commit as the fixed point and the active ticket/specification as the Spec source. Run independent Standards and Spec review axes. Fix all blocking/high-confidence findings, retest, and amend the same ticket commit. Repeat review if a fix materially changed behavior. The final range `$base_commit..HEAD` must still contain exactly one commit.
8. Run the final focused and full applicable test suites after the last fix. Finish with a completely clean worktree, including no untracked files.
9. Write a UTF-8 JSON receipt to $receipt_path after the final commit and tests. It must use this schema and report real commands/results, not intentions:
   - `version`: 1
   - `ticket`: "$ticket_id"
   - `nonce`: "$nonce"
   - `base_commit` and `final_commit`: full Git SHAs
   - `result`: "complete"
   - non-empty `plan` array
   - non-empty `tdd_cycles` array; every item has non-empty `test`, `red`, and `green`
   - non-empty `focused_tests`, `full_tests`, and `final_tests` arrays; every item has `command` and `result: "passed"`
   - `precommit_review` with `status: "passed"`, `findings`, and `fixes`
   - `code_review.standards` and `code_review.spec`, each with `status: "passed"` and `findings`
   - empty `unresolved_blockers` array
10. Your final response must end with exactly `TICKET_RESULT: COMPLETE`.

If blocked or any required evidence cannot be produced, do not mark the ticket resolved and do not claim success. Preserve useful diagnostics, avoid committing partial work, and end with `TICKET_RESULT: BLOCKED` plus the reason immediately before it.
